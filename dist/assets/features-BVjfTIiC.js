const s={live:"LIVE",partial:"PARTIAL","not-built":"NOT BUILT"},o={live:"LOW",partial:"MEDIUM","not-built":"NOMINAL"},a=[{id:"engine",label:"Conjunction screening engine",status:"live",group:"Operations",note:"Real SGP4 (satellite.js) over real element sets. Coarse 60 s sweep with a 450 km gate derived from the step size and the ~15 km/s maximum closing speed, then bisection on the sign change of range rate for the exact time of closest approach. Miss distance and relative velocity are measured at that instant. `npm run validate` checks it against brute-force all-pairs and known ISS geometry."},{id:"data",label:"Orbital data",status:"live",group:"Operations",note:"A committed CelesTrak GP snapshot — 859 real tracked objects across five groups, all captured at one instant so their epochs are mutually consistent: the ISS and CSS stations, ISRO’s LEO fleet, and the Cosmos 1408, Iridium 33 and Cosmos 2251 debris clouds. Bundled into the build, so the console makes no network request at any point. The console clock is anchored to the capture instant, because propagating these element sets from today would be arithmetic rather than prediction."},{id:"consequence",label:"Consequence analysis",to:"/console/analysis",status:"partial",group:"Operations",note:"A research workbench, and a summary panel on every event. Impact mechanics are taken in the centre-of-mass frame (the only frame in which the energy means anything), the NASA Standard Breakup Model produces the cloud, each parent’s fragments are ejected about that parent’s own real SGP4 state with momentum conserved per sub-cloud, King-Hele drag gives orbital lifetime, differential J2 precession gives how long the cloud takes to become a shell, and Sutton-Graves heating with a lumped-mass demise criterion decides which fragments survive re-entry at all. Every assumption that moves the answer — both masses, the material mix, drag coefficient, solar activity, entry angle — is a control rather than a constant, and the fragment-level output exports to CSV. Partial for three honest reasons. Object MASSES are assumed from class, and fragment count scales as mass^0.75, so that assumption propagates into every figure. Only fragments at or above 10 cm are modelled — the real cloud holds orders of magnitude more debris than any catalogue tracks. And lifetimes are order-of-magnitude only, because atmospheric density varies by more than 10x over the solar cycle and solar activity is not modelled; they are reported in bands, never as dates. What is NOT approximate is the re-entry latitude bound: an orbit never crosses its own inclination, and the latitude distribution inside that band is a closed form. Re-entry LONGITUDE is not predicted at all, because it is not predictable."},{id:"cascade",label:"Cascade risk",to:"/console/analysis",status:"partial",group:"Operations",note:"Closes the loop the console is named after: the modelled debris cloud is fed back as an environment and the ADDED collision rate on every crewed station and ISRO asset is computed, using the particle-in-a-box formulation of Kessler & Cour-Palais (1978) — rate = n · v · A. Shell occupancy is weighted by the time a fragment actually spends at each altitude (dt proportional to r squared), not by its perigee-apogee span, and closing speed is averaged over a uniform node distribution rather than assumed. Partial, and the limits bound the numbers. The gas picture only holds once nodal precession has spread the cloud around the Earth, which takes the number of days reported beside it; before that the debris is a clump and the risk is concentrated, so these figures are a floor for the first weeks. Cross-sections are assumed by size class because a TLE carries no geometry, and rate is linear in cross-section. Only fragments at or above 10 cm are modelled. What is reported is the INCREMENT from one event, never total environmental risk. Deliberately not a conjunction screen: days after a breakup a fragment’s position around its orbit is unknown, so naming specific future close approaches would be inventing precision the model does not contain."},{id:"gabbard",label:"Gabbard diagram",to:"/console/analysis",status:"live",group:"Operations",note:"Apogee and perigee of every modelled fragment against its orbital period — the standard published view of a breakup cloud. Period is derived from the perigee and apogee already computed for each fragment, so the plot introduces no new modelling of its own. Fragments whose perigee has been driven into the atmosphere are drawn on the floor of the plot and are the same population the panel counts as immediate re-entries."},{id:"covariance",label:"Positional uncertainty",status:"partial",group:"Operations",note:"A TLE carries no covariance, so the 1-sigma used for Pc is ASSUMED, not measured: it grows with real element-set age and with a radar cross-section class inferred from object type, since RCS lives in the SATCAT and not in a TLE. Both assumptions are disclosed on the detail view. The Thresholds screen exposes the sigma as a scale control and re-bands every event live, so you can see how much of the severity ranking rests on it."},{id:"conjunctions",label:"Conjunctions",to:"/console",status:"live",group:"Operations",note:"Ranked event table over real SGP4-screened conjunctions. Severity, window and class filters and every column sort genuinely filter and re-sort the data. Rows open the detail view."},{id:"detail",label:"Conjunction detail",to:"/console",status:"live",group:"Operations",note:"Separation chart, both object specification panels, encounter geometry, the risk-model breakdown and the data-quality disclosure. The separation curve is propagated on demand from both element sets, not fitted. Export report downloads this single event as CSV. Reached by opening any row."},{id:"viewer",label:"Orbital viewer",to:"/console/viewer",status:"live",group:"Operations",note:"Time scrubber, play/pause, rate control and layer toggles all drive the canvas, on the same snapshot-anchored clock as the rest of the console. Clicking an object selects it. The orbits themselves are a SCHEMATIC: circles drawn from each object's real inclination, RAAN and mean motion, not SGP4 output. Positions are right to a glance, not to a kilometre — the conjunction geometry on the detail view is the propagated one."},{id:"catalogue",label:"Object catalogue",to:"/console/catalogue",status:"live",group:"Operations",note:"The whole screened catalogue, searchable, sortable and paginated. Element sets are the real committed TLEs; selecting a row opens the annotated set."},{id:"manoeuvres",label:"Manoeuvre log",to:"/console/manoeuvres",status:"live",group:"Operations",note:"Two halves, and they are not equally real. The burn HISTORY is synthetic and read-only — no manoeuvre in that table was ever planned or flown. The burn ADVISOR beside it is live and now genuinely re-propagates: the delta-v is applied to the asset’s real state vector, the burned and unburned states are both propagated, and their difference is a true three-dimensional displacement — so it gives one post-burn miss distance rather than a range, re-derives Pc and severity from it, and searches for the new time of closest approach rather than assuming the burn leaves it unchanged. The closed-form estimate is shown beside it for comparison. Two limits remain: the displacement arms are two-body (their model error cancels between them, the absolute trajectory is still SGP4), and this is not a re-screen — nothing checks whether the burn creates a new conjunction with a third object."},{id:"thresholds",label:"Screening thresholds",to:"/console/thresholds",status:"live",group:"Configuration",note:"Sets the screening floor and the assumed positional uncertainty. Changes apply immediately to the dashboard and the manoeuvre log, and persist across reloads."},{id:"signin",label:"Sign in",to:"/signin",status:"partial",group:"Account",note:"The form validates and sets a local display name used by the console. There is no backend, so nothing is authenticated and no credential is checked or stored."},{id:"assets",label:"Asset register",to:"/console/catalogue?isro=1",status:"partial",group:"Configuration",note:"You cannot yet declare an arbitrary set of objects as yours. One fixed register does exist and is real: the ISRO-operated LEO fleet ingested as its own snapshot group, filterable on both the dashboard and the catalogue, and matched on ingest group rather than on a name string so it cannot drift if an object is renamed in a later capture. The sidebar entry opens the catalogue with that filter already applied. 77 events in the committed run involve one of those assets; 72 of them survive the default thresholds and appear on the dashboard."},{id:"alerts",label:"Alert rules",to:"/console/alerts",status:"partial",group:"Configuration",note:"Standing rules that pick events out of the screening run by minimum severity, miss distance, lead time and whether an ISRO-operated asset is involved. The matching is real: it runs against the same rebanded events every other screen shows, under the operator’s own covariance assumption, and updates as the thresholds move. Rules persist in this browser. Partial for one reason, stated on the screen itself rather than in a footnote: DELIVERY is not built and cannot be. Email and webhook routing needs a server, and this console has none by design — the whole run is bundled and no network request is made at any point. A rule tells you what it caught; it does not tell anyone else."},{id:"export",label:"Export CSV",status:"live",group:"Actions",note:"Downloads the conjunction table exactly as filtered and sorted on screen: event id, TCA in UTC, miss distance, relative velocity, Pc, score, severity, sigma, element-set age and both objects. Full precision, not display precision. Where the page is embedded somewhere that sandboxes downloads, it asks the host to save the file instead."},{id:"run",label:"Run screening",status:"live",group:"Actions",note:"Runs the real screening engine in a Web Worker with live progress: SGP4 propagation of every catalogued object over the configured horizon, a coarse distance screen, then bisection on range rate for each candidate. A 72 h run is about 3.6 million propagations and takes roughly twenty seconds. It reproduces the committed result exactly, because it is the same code. One exception, which the button reports rather than swallowing: a page opened straight from disk (file://) is a null origin, where the browser refuses to start a worker at all."},{id:"ack",label:"Acknowledge event",status:"partial",group:"Actions",note:"Marks an event acknowledged from either the dashboard or the detail view and remembers it across reloads. Acknowledged rows are dimmed and tagged ACK in the table, and the detail view carries a chip. Still partial, not live: there is no backend, so nothing is recorded against an operator, nothing is sent, and no one else can see it — it is local to this browser."}],n=a.reduce((e,t)=>({...e,[t.status]:e[t.status]+1}),{live:0,partial:0,"not-built":0});export{n as C,a as F,s as S,o as a};
